cmake_minimum_required(VERSION 2.6)

PROJECT(MaskImage)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

ADD_EXECUTABLE(MaskImage MaskImage.cxx)
TARGET_LINK_LIBRARIES(MaskImage
ITKBasicFilters ITKCommon ITKIO)
