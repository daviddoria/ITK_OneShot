cmake_minimum_required(VERSION 2.6)

PROJECT(AndImages)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

ADD_EXECUTABLE(AndImages AndImages.cxx)
TARGET_LINK_LIBRARIES(AndImages ${ITK_LIBRARIES})
